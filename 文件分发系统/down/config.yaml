# ===========================================

minio:
  endpoint: '47.99.52.9:9000'
  access_key: 'admin'
  secret_key: 'Aa123456.'
  secure: false  # Https协议

# ===========================================

buckets:
#  - name: 'test'
#    bucket_dir: "text1/"  # 存储桶中的一个路径

  - name: 'zyb'
    bucket_dir: "zuoyebang-SQ-3/"  # 存储桶中的一个路径

# ===========================================

local:
  directory: 'D:/bucket'  # 客户端: 此文件夹必须存在
  #   注意格式必须标准!
  local_dir: 'D:/bucket/{bucket_name}/{bucket_dir}'  # 本地位置模板，根据存储桶名称动态生成:

  # ===========================================

settings:
  max_workers: 30  # 线程（未启用)
  schema: 'v1'  # (模式 v1: 单文件模型, 模式 v2: 整体文件模型)
# ===========================================

confWay:
  WHITELIST_FILE: "D:/whitelist.json"
  PROGRAM_KEY: "zzff"  # 当前程序的标识键

# ===========================================

# 14:30:22 | INFO     | 🔄 开始同步文件夹 [zuoyebang-SQ-3/] 共 35 个文件
# 14:30:25 | DEBUG    | 📥 下载进度 | 352_20_part... | 45.6% | 102MB/223MB
# 14:30:27 | SUCCESS  | ✅ 完成下载 | 352_20_part1.xlsx
# 14:30:28 | ERROR    | 🚨 352_20_part2.xlsx ❌ 连接超时
# 14:31:02 | INFO     | 📦 文件夹同步完成 | 成功: 33/35
# 📁
# 🚀